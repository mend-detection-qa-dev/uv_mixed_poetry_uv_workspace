# New Service - UV Only (Fully Migrated)
# This package has been fully migrated to UV

[project]
name = "new-service"
version = "1.0.0"
description = "New service using UV"
requires-python = ">=3.10"

dependencies = [
    "fastapi>=0.109.0",
    "pydantic>=2.0.0",
    "sqlalchemy>=2.0.0",
    # Depends on shared-lib (internal)
    "shared-lib",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "httpx>=0.27.0",
]

[tool.uv.sources]
shared-lib = { workspace = true }
